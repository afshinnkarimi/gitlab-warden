use std::vec::Vec;

struct Token {
    id: i32,
    name: String,
    revoced: bool,
    created_at: String,
    scopes: Vec<String>,
    user_id: i32,
    last_used_at: String,
    active: bool,
    expires_at: String
}

fn main() {

}


